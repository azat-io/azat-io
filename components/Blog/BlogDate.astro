---
import Date from '~/components/Date.astro'

interface Props {
  date: Date
}

let { date } = Astro.props
---

<Date class="text" date={date} />

<style>
  .text {
    font: var(--font-xs);
    line-height: 1 !important;
    color: var(--color-content-primary);
  }
</style>
