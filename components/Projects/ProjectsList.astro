---
let { props } = Astro
---

<ul class="list" {...props}>
  <slot />
</ul>

<style>
  .list {
    display: grid;
    gap: var(--space-l);
    padding: 0;
    margin: 0;
    list-style: none;

    @container (inline-size >= 560px) {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
